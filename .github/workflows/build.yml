name: build
on:
  - push
jobs:
  build_image:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: "Build Image"
        run: docker image build --tag=project_jee:latest ./